import { defineCliConfig } from 'sanity/cli'

export default defineCliConfig({
  api: {
    projectId: 'k118i4e6', // Replace with your project ID
    dataset: 'production',
  },
  studioHost: 'sadeiriah',
})
